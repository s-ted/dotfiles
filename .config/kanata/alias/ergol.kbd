(defalias
    aa (tap-hold $tap-timeout $hold-timeout a lalt)
    ss (tap-hold $tap-timeout $hold-timeout s lctl)
    ee (tap-hold $tap-timeout $hold-timeout e lsft)
    nn (tap-hold $tap-timeout $hold-timeout n lsft)
    rr (tap-hold $tap-timeout $hold-timeout r lsft)
    tt (tap-hold $tap-timeout $hold-timeout t lctl)
    ii (tap-hold $tap-timeout $hold-timeout i lalt)
    uu (tap-hold $tap-timeout $hold-timeout u lmet)
)
